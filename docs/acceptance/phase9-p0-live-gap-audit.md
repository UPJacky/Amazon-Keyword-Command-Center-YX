# Phase 9 P0：真实 run 六模块缺口基线

日期：2026-09-07

## 审计对象与边界

- task：`a32651d2-63cd-4466-8e60-9d79c182e516`
- run：`ec431cab-f078-4abd-b607-d74f61ac8b0a`
- 本轮只读，不修改线上任务、报告、用户、权限或对象；不记录令牌、对象路径或任何 Secret。

历史带地址栏截图和既有生产验收可确认：该 run 已完成、主表 91 行、广告花费 5,123.10 USD、订单 1,578、CTR 0.7%、广告对账差值 0；页面同时明确标注“广告数据报告，市场、竞品与图片证据尚未补齐”。因此 task/run 参数无误，缺数不是用户打开方式造成的。

本次尝试通过已授权普通测试账户重新读取私有 bundle，但两个测试账户当前均无法重新取得会话；未改变密码，也未绕过鉴权。SSH 默认密钥入口同样未认证成功，未继续重复。以下模块基线由该 run 的不可变验收记录、对应发布版生产打包契约和用户当前逐页现象交叉确认。

## 六模块基线

| 模块 | bundle/数据状态 | 可用业务数据 | 结论 |
|---|---|---|---|
| 01 关键词作战总表 | `rows` 91 行 | 广告展示、点击、花费、订单及确定性规则动作 | `ready/ad_only`；不能代表市场数据完整 |
| 02 自然位标杆 | 发布版强制打包 `rank-benchmark.json` | 广告行存在，但自然位、广告位、Top3 和三标杆输入没有被生产入口采集 | `partial`；页面有文件不等于有排名事实 |
| 03 否定词清单 | 发布版强制打包 `negative-keywords.json` | 可从广告点击/订单/花费生成候选 | `partial`；缺相关性保护和词组碰撞保护，不能直接执行 |
| 04 竞对对比 | `competitors.json` 仅在显式注入档案时打包 | 本次生产入口没有按任务采集竞对 | `not_generated` |
| 05 图片与卖点诊断 | 发布版 `_bundle` 不读取 `listing-diagnostics.json` | 独立本地 CLI 的 unknown/checklist 不在生产报告链 | `not_generated` |
| 06 广告诊断与优化 | 发布版强制打包 `optimization-plan.json` | 只有关键词级规则动作投影 | `partial`；缺 campaign/ad group/target/match type 实体诊断 |

## Worker 模式结论

当前生产 CLI 只有 `--ad-only` 与受限的 `--xiyou-keywords 1..10` 两种显式模式；后者也只调用 `get_keyword_info`，两种模式都固定输出 `full_report_complete=false`。该真实 run 的页面范围声明与 `ad_only` 一致。无论服务器服务文件当下是否已被改写，这个不可变 run 本身都是 ad-only；旧 run 不应覆盖，修复后必须创建新 run 验收。

## P1-P8 输入基线

- P1 建立模块状态、缺失原因和 task/run 上下文契约。
- P2 补 Provider 字段映射、跨层保留、`my_asin` 和按真实排名选三标杆。
- P3 加相关性与词组碰撞保护。
- P4/P5 把按任务竞对和真实图片诊断接入生产 bundle。
- P6 在输入粒度允许时保留广告实体；缺明细时明确不可判断。
- P7 只在数据状态契约稳定后重构样式。
- P8 发布后新建 run 逐页验收，不覆盖本次历史 run。

Phase 8 的鉴权、私有报告、未登录拒绝、Pages 路由、广告对账仍然有效；“整个项目完成”结论撤销。六模块业务完整性只有在新 run 中逐页出现真实数据或有可追溯的 `partial/not_generated/failed` 原因，并通过 P8 安全验收后才能宣布完成。
