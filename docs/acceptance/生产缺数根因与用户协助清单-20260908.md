# 生产缺数根因与用户协助清单

日期：2026-09-08。依据：当前工作区源码、LUNU-04 和本机配置存在性检查。本轮没有调用收费接口、修改生产服务或读取输出密钥值。

## 1. 纠正此前结论

“本地全部完成，只需等待 Provider 返回数据”不成立。已有本地单元测试并未证明完整采集链已接入。不能依靠重复运行监督器、现有测试或等待第三方自动返回来解决。

本轮已修正图片模块“任意观察就 ready”的误判：原始观察组保持 partial，兼容构建器在缺少确认要素与完整视觉矩阵时保持 partial；相应错误测试预期已修正。使用应用自带 Python 运行图片清单、视觉证据和生产 runtime 共 48 项测试通过。系统默认 Python 缺 openpyxl，首次 XLSX 测试失败；切换已有完整运行环境后通过，未掩盖失败或安装新依赖。

其余采集与视觉接入问题仍未修复；本文不是六模块完成证明。现有任务图 completed 回执不能覆盖这些新发现。监督器已原子领取 production-adapters-local，需继续完成接入复核，不能重复宣布本地全部完成。

## 2. 已定位的原因

| 问题 | 源码证据 | 必须实施的修复 |
|---|---|---|
| 市场数据覆盖不足 | worker/providers/xiyou_live_enrichment.py 中 max_keywords 限制为 1–10，selected 只取前 N 个词；基础字段投影只保留部分响应 | 根据已验证工具 schema 接入所需排名、份额、趋势；全量分批、预算和缓存必须有明确状态，未采集词不能当成无数据词 |
| 产品详情/类目特征来源未接通 | 当前 worker/providers 下没有 Sorftime adapter；非测试 Python 源码搜索未找到 Sorftime 实现 | 确认实际可用的数据渠道及 schema，接通详情、图组、Listing 文本；类目特征要单独验证能力，不能假定产品详情接口包含 |
| 图片只传 URL，没有观察数据 | worker/pipeline/task_runner.py 构造 self_images/competitor_images 时只传 ID、URL、位置、来源、时间 | 接入真实视觉调用，把有版本、图片 ID、要素 ID 的结构化观察传入报告；保留失败、缺图和 unknown |
| 任意观察可能误报 ready | worker/diagnostics/listing_checklist.py 使用 any(observations) 判定组 ready，模块仅依据自有组状态 | 使用确认后的要素清单与全图覆盖校验；任意观察、任意描述、单张成功不得判整模块 ready |
| 已有测试认可错误状态 | worker/tests/test_listing_checklist.py 的 observations 测试预期单条自由描述让模块 ready，同时 checklist 全 unknown | 改成业务反例：无结构观察、缺图、缺要素、竞品未分析、检查表 unknown 都不应整体 ready；增加完整证据正例 |
| 完成审计过宽 | E06/E13 等回执以通用测试通过支持阶段完成 | 重开受影响任务，完成实际调用入口→模型/Provider→持久化→前端的契约和真实新 run 验收 |

补充：此前浏览器直达报告后仅观察到空 RootWebArea，不能证明鉴权有效。重新验收必须等待加载结束，确认跳转地址/明确拒绝，并同时验证匿名接口拒绝。HTTP 200 或空白均不够。

## 3. 你现在只需协助确认数据渠道

### 必需：Sorftime 是否已有账号或 MCP 配置

1. 在你平时使用的浏览器打开已有 Sorftime 产品或账号页面。
2. 如果已经有 MCP/API 配置入口，查看是否已有可用配置；不用更换旧账号或重置密钥。
3. 把配置保存到本机现有私密凭据文件，或你自行指定的新私密文件；不要贴到聊天，也不要提交 Git。
4. 在聊天只回复：“Sorftime 已有，配置文件路径是……”，或“目前没有 Sorftime”。
5. 如果找不到入口，回复“已登录 Sorftime，入口找不到”，由 Agent 检查浏览器后继续；不要求你自行编写 API 请求。

本机已检查：当前进程及用户环境没有 LINKFOXAGENT_API_KEY；用户指定的项目凭据文件未提及 Sorftime 或 LinkFox。该结果不证明服务器或其他文件没有配置。

备选渠道：本机安装的 LinkFox Sorftime 产品详情技能描述了一个 LinkFox 网关，需要独立 LINKFOXAGENT_API_KEY。技能存在不代表账号已授权；此渠道也不等同于 Sorftime 原生 MCP。不要为本项目先购买或开通新付费服务，先确认已有渠道。

### 现阶段不用你重新提供的内容

- 豆包：现有项目凭据文件已包含相关配置标记，Agent 后续从文件安全读取并验证，不要求重新发密钥。
- 西柚：现有 MCP 配置文件已经在项目内，不要求重新申请。
- Supabase：不要求更换项目、重建测试用户或重置密码。
- 服务器：只有实际连接遇到登录过期、扫码或验证码时，再由你完成那一步。

## 4. 接通后可能需要的业务确认

Agent 应优先从真实任务读取下列值，只有缺失或存在冲突才向你核对：

1. 自有 ASIN、站点、核心关键词、希望比较的竞品 ASIN；不能使用参考截图中的产品冒充你的任务。
2. 买家决策要素清单：Agent 根据真实关键词、类目特征和已获评论提出 5–7 项草案，注明依据；你确认后生成确认版本。
3. 若广告诊断需要当前广告文件不含的实体 ID、预算或出价，Agent 先列出具体缺列，再说明需要导出的报表种类；不要求你笼统上传所有广告数据。

## 5. Agent 后续执行顺序

1. 重开 E06/E13 等被新证据否定的任务；不得沿用现有 completed 结论。
2. 修正 listing ready 判定和错误测试，接通已有 visual_evidence 校验，保证证据完整性。
3. 验证现有数据渠道 schema；有直接 MCP 时优先复用，禁止凭参考图片编造接口名称和字段。
4. 补齐 Provider 产品资料、排名、图组及视觉调用入口；复用现有凭据，记录预算和来源，不把临时手工注入当自动流水线完成。
5. 对局部失败、缓存复用、不同 task/run 隔离、图组覆盖、模型无效输出做受影响测试；不重复无关全量审计消耗时间。
6. 部署前核对运行服务、代码 hash 和配置存在性；不回显 Secret。
7. 仅创建一个新真实 run，等待当前任务结束；失败先定位，不能重复提交相同任务。
8. 读取私有报告，逐页验证数据和判断依据，对账零差值，抽词、抽图、确认版本、匿名拒绝和跨店读取均有证据后再关闭 E19/E20。

## 6. 恢复时的上下文管理

先读本文、PROJECT_STATUS.md 顶部、LUNU-04 当前任务卡和相关源码；不从多年历史测试计数推断当前完成度。每次交接记录：实际改动、真实输入范围、测试证据、未完成项、下一步；敏感响应保存在 Git 外。没有真实调用或实际可检查产物时，明确写“未验证”。
