# 项目审核验收与逐项修复执行清单（Lunu 执行版）

审核日期：2026-09-11。项目：Amazon-Keyword-Command-Center-YX。
审核代码基线：`f1e69e9a0462eac72cdf36a33ea2d32ed1cc424b`。
本轮产出性质：审核、离线验证、公开资源核对和修复方案。**本轮没有实施下列业务修复、没有修改数据库、没有重新部署，也没有重开任务图。**

## 0. 先读结论：六模块业务验收未通过

之前“46/46 任务完成、项目全部完成”的表述不符合 LUNU-04 第13.3节，必须纠正。已有生产链路和局部能力可以保留，但目前仍存在实现漏接、判断错误和验收覆盖不足。

这不是要求把所有 null 改成数值。允许真实无结果、预算不足、待确认；但必须证明相应能力已经接通并实际执行，不能把没有采集器、没有调用模型、没有传入配置统称为供应商缺数。

**最直接的审核证据：**

- 同一个词的两个广告实体，一个花费10、订单10，另一个花费100、订单0；当前代码给两者相同的 `scale_up` 动作，而且退出条件 pending 时模块仍为 ready。
- `rope lights` 不相关、`indoor rope lights` 相关且高花费；当前词组否定仍允许导出前者，相关保护被分组流程漏掉。
- 一条记录同时缺 clicks 且相关性 unknown，当前否词覆盖数得到 `-1 / 1`。
- 自有商品只有 ASIN、3家竞对有完整档案，竞对模块仍能声明 ready。
- 现有单张主图与 Sorftime 返回的3张图片合并后，仍只留下旧的1张。
- 生产 Worker 构造 Provider 时只传5个字段，遗漏主要核心词、指定竞对、清单和确认版本；工具页和提交 RPC 也没有完整业务输入。
- 视觉代码有接收外部观察结果的入口，但当前生产工厂没有调用视觉模型；不能称为“视觉供应商没有数据”。

证据摘要保存在 `docs/acceptance/project-review-20260911.json`。以上为本轮实际运行的离线反例和源码结论；合成数据只用于验证程序行为。

### 0.1 已通过与未验证的边界

| 项目 | 本轮结果 | 不能由此推导的结论 |
|---|---|---|
| 现有完整本地 UAT | 返回0，20/20 Gate；Worker 325、前端 Python 包装测试8 | 不能证明缺失的业务案例已通过 |
| 9个公开页面/静态资源 | HTTP200，逐字节与当前 frontend 文件一致 | 不能证明登录后报告内容、跨店隔离和手机视觉效果 |
| 原广告聚合、实体原始量聚合、私有报告链路代码 | 已有可复用实现与回归 | 实体有事实不等于实体动作正确 |
| 2026-09-10 真实 run | 本轮查阅历史记录，记录为 live_provider_data 且 full_report_complete=false | 本轮未重新登录取私有报告，不能称2026-09-11线上全链复验 |
| 生产数据库、当前服务器服务、当前权限矩阵 | 本轮未执行新的数据库/服务器实测 | 007是否实际应用、确认RPC实际边界仍需后续验收 |
| 桌面/手机/无痕截图 | 本轮未新增浏览器截图 | 旧截图不替代修复后同版本新run的截图 |

公开抽查文件：`index.html`、`tool/index.html`、`tool/tool.js`、`report/rank.js`、`report/listing.js`、`report/competitors.js`、`report/negative.js`、`report/optimization.js`、`styles.css`。哈希在证据 JSON。不能再把这些代码缺口解释为“用户没有刷新缓存”。

历史生产样本仅作复验定位：task=`f14e96ea-84ad-4d1e-99dc-2cf38ccf24c4`，run=`f01faf00-67d2-4e1b-b1d1-71388496a9ff`。修复后创建新run，不覆盖这个样本。

### 0.2 本文使用方式

1. 用户明确让你实施本文后，才启动修复；仅阅读方案不修改生产。
2. 先读本节、第1节和当前任务卡；不要每轮重读全部历史 MEMORY。
3. 下方路径均相对项目根目录。行号只对应审核基线，修改后用函数名定位。
4. 每张卡必须有“失败案例→实现→行为测试→产物验证→短交接”，不能只改文案或记录通过。
5. 普通本地修改、测试和文档同步按既有授权推进；不反复询问“是否继续”。涉及实际外部调用时先核对既有授权、当前预算和凭据可用性，不输出 Secret。
6. 原计划与本文共同适用；本文件具体反例优先于旧的“完成”记忆。真实无数据依然保持 unknown/partial。

## 1. 任务分组与执行顺序

优先级：P0=阻止误验收或避免错误运营动作；P1=必需能力缺失或证据不可靠；P2=交互和展现完善。P0不表示已经发生资金损失或数据泄漏。

| 卡 | 优先级 | 工作 | 主要前置 |
|---|---|---|---|
| R01 | P0 | 纠正完成状态、修复任务证据与指纹范围 | 无 |
| R02 | P1 | 工具输入→RPC→claim→Worker→报告全链 | R01 |
| R03 | P1 | 确认记录的任务归属、不可变版本和真实确认来源 | R01；与R02协同 |
| R04 | P1 | 排名/份额/ABA采集接线、指定竞对与采样范围 | R02、R11、R12 |
| R05 | P1 | Sorftime完整图组、字段/来源合并和自有档案状态 | R02 |
| R06 | P1 | 类目文本证据和买家清单实际接入 | R03、R05、R11 |
| R07 | P1 | 视觉实际调用、观察校验、规则判定和逐图brief | R06、R12 |
| R08 | P0 | 否词保护、覆盖计数、词根及确认复制 | 后端保护可先做；交互依赖R03 |
| R09 | P0 | 实体独立规则判断和指标保留 | R01 |
| R10 | P0 | 主规则缺口、防守上限、观察退出和追溯 | R09；确认参数依赖R03 |
| R11 | P1 | 份额分母、数值单位和缺失语义 | R01 |
| R12 | P1 | 生产缓存、重试、预算耗尽和选择性重跑 | R01、R02 |
| R13 | P1 | 持久证据、真实调用来源和失败回执 | R02、R12；覆盖所有模块 |
| R14 | P2 | 六模块业务界面与同run导航 | 按模块依次依赖R04–R11 |
| R15 | P0 | 新验收Gate、真实新run、权限与截图终验 | 全部必要本地卡 |

推荐顺序：R01→R08后端→R09→R10规则基础→R11→R03→R02→R05→R12→R04→R06→R07→R08交互→R10退出→R13→R14→R15。
这不是让同一任务重复领取。把分段卡拆为独立子任务ID并声明依赖；一张卡已有完整证据后不重复执行。

## 2. R01：完成状态和任务回执不能代替业务验收

**证据与原因**

- `PROJECT_STATUS.md:7` 声明 complete，第13行同时记录排名位次缺失、图片无观察、优化退出缺失。
- LUNU-04 第13.3节明确禁止“必需采集器没接 runtime”时关闭目标。
- `PROJECT_TASKS.json` 的 E04/E05/E09/E12/E18 等回执重复引用通用 UAT/静态检查，无法证明对应能力接入。E20虽然列出了缺失，却写成 passed。
- 本轮解析任务图：20张 LUNU-04 卡均没有 `supabase` 指纹输入；E05对应迁移、E19对应生产入口，相关 SQL、scripts、rules 未逐卡声明。大部分卡却把整个 frontend/worker 都纳入指纹，容易造成不相关改动引发重复全量验证。
- 监督器只能处理声明的条件和回执，不会从业务结果中自动理解任务是否完成。修复重点是验收条件、证据和输入范围，不能只指责监督器。
- 本轮收尾只读运行监督器返回 `RUNNING`，下一项为 `production-adapters-local`、`stale=true`，而磁盘 PROJECT_STATUS 仍写 PROJECT_COMPLETE。该任务把整个 `docs/acceptance` 纳入输入，本轮新增审核JSON也会触发失效；这是输入范围过宽的具体例子，不是源码已经被修复或测试新失败。本轮仅审核，没有执行 `--claim`，没有修改任务图；实施R01时应核清哪些失效需要行为重验、哪些只是无关审核文档变化，禁止直接重跑全部任务或盲改hash。

**实施步骤**

1. 先保留旧台账/验收记录作为历史；新增审核纠正记录，重开受影响能力或建立本轮R任务图，不删除已经验证的认证、解析、私有存储工作。
2. 区分 `pipeline_status`（任务跑完）、`business_acceptance`（必要能力通过）、各模块 coverage/status。不要将数据库 run 的 completed 当成业务完整。
3. 按当前小卡声明源码、SQL、rules、入口脚本、契约及直接测试文件。不可只修改 hash 使旧回执复活。
4. 每个业务Gate引用具体测试案例ID、产物字段路径、断言和当前输入指纹。E20必须有第6节终验矩阵逐项结果。
5. 同步 PROJECT_STATUS/MEMORY/PROJECT_MEMORY 的最新短结论：当前六模块验收未通过，仍有安全本地修复。历史条目加明确日期，不用重写全部历史。
6. 使用监督器现有领取/完成接口；确需支持重开时以经过验证的任务图变更执行，遵守并发锁，不并行手改running状态。

**通过标准**：修改007/后续迁移能使相关Gate过期；修改视觉规则只重验受影响卡；本轮P01/P07等失败时业务终验必失败；仅声明partial不能使E20通过。

## 3. 分项修复任务卡

### R02：业务输入在提交链和生产工厂丢失

**定位**：`frontend/tool/index.html`、`frontend/tool/tool.js:60`、`frontend/client.js`、`supabase/functions/kwcc-gateway/gateway.mjs`、`supabase/migrations/005_production_jobs.sql:83`、`007_task_business_inputs.sql`、`worker/tasks/task_model.py:23`、`worker/runtime/production.py:527`、`scripts/run_production_worker.py`。

**实际问题**：工具表单仅有自有ASIN/阶段/店铺/上传；create调用不带竞对或核心词。005提交RPC不接收这些新增参数，007只新增列和确认函数，没有补提交链。runtime给provider_factory只传task_id/store_id/self_asin/product_stage/marketplace，因此CLI虽读取 `primary_core_keyword`，实际拿到None。Task dataclass也没有新增确认版本字段。

**实施步骤**

1. 写一份共享业务输入契约：competitor_asins、core_keywords、primary_core_keyword、competitor_selection_version、product_facts_version、feature_review_version、checklist_version及已有strategy身份。
2. UI增加输入；ASIN转大写、去重、排除自身，按本期统一B0+10位校验；允许计划中的3–5家竞对，不能仍被profile默认max_count=3拒绝。最多三自然标杆与最多五指定竞对是两个上限。
3. core_keywords保留用户意图，primary须属于确认清单。没有primary且没有可靠流量依据时提供待选择，不以报表首词冒充核心词。
4. 使用新的编号迁移扩展提交RPC和幂等比较；同步client/Gateway参数白名单，禁止开放任意RPC转发。旧签名兼容要具体测试。
5. 新run冻结业务输入快照和确认引用。claim将经过授权的快照传出；runtime明确白名单传到provider_factory/run_task，禁止传lease/认证凭据。
6. 业务输入变更必须创建新快照/新run；仅给Task表加可修改列不能保证历史重现。

**行为测试**：同一上传文件、不同指定核心词/竞对，传输spy捕获正确参数，两个run的快照不同；两个店铺不串档；只读旧任务不报错；重复提交同ID同内容幂等，不同内容冲突。

### R03：确认RPC缺任务归属验证，确认版本可覆盖

**定位**：`supabase/migrations/007_task_business_inputs.sql:33` 的 `kwcc_save_task_confirmation`；`worker/diagnostics/buyer_checklist.py:9`；确认消费者和Gateway。

**已证实的源码缺陷**：SECURITY DEFINER函数只验证p_store_id权限，没有验证p_task_id所属store/self_asin/input_hash。外键只保证任务存在。相同object/version/store冲突时直接UPDATE确认内容和身份，会破坏历史版本不可变性。buyer_checklist只要confirmation_version非None就把条目标记confirmed。

**证据边界**：本轮没有在真实数据库尝试越权写入。这里是SQL静态缺陷，不能说已经窃取其他店铺数据；后续消费者接入后会造成错误绑定和确认污染风险。

**实施步骤**

1. 新迁移校验任务存在、store/self_asin一致、调用者有该任务店铺权限、input_hash匹配冻结对象。对NULL显式检查，不能依赖SQL三值逻辑。
2. confirmed对象只插入不可变版本。相同版本完全相同请求返回原记录，内容不同返回版本冲突；不能覆盖confirmed_by/confirmed_at。
3. object_id、version、items数量/长度、ASIN、hash格式设系统校验；确认种类白名单和适用范围明确。
4. 服务端生成确认人/时间。消费者读取受权确认对象并核对hash/status/scope，而不是把任意版本字符串当已确认。
5. 用户委托的业务决定记录delegation依据和执行身份，不能伪造逐条人工点击。旧确认对象只读并标可信度，不暗中“补签”。

**通过标准**：真实或一次性本地Postgres测试A店+ B任务拒绝、自己任务错误ASIN/hash拒绝、匿名拒绝、并发同版本冲突拒绝、不同新版本可保存、旧报告确认内容不变。SQL字符串包含测试只作辅助。

### R04：自然位和份额没有完整的生产采集链

**定位**：`scripts/run_production_worker.py`、`worker/providers/xiyou_live_enrichment.py:147/207/234/284/325/372`、`worker/providers/market_merge.py`、`worker/report/modules.py:71/125`。

**实际问题**

- 生产工厂只构造XiyouLiveEnricher；关键词数据主要映射难度和单周ABA基础字段。rank、benchmark、share、ABA历史等始终空，不能认定是供应商“明确不提供”。
- get_keyword_asin_analysis只为首个抽样词调用，结果只投影产品信息，ranks/traffic相关字段没有进入rank产物。
- `next(iter(selected.values()))` 决定实际竞对查询词；即使单独传入primary，也主要改变返回标签，查询不保证使用primary。
- 用户指定竞对名单没有传入；候选固定取流量前5再截3，不能冒充用户选择或点击前三覆盖推荐。
- merge_market_data只遍历广告行，额外市场词被丢弃，没有market_only扩词视图。

**实施步骤**

1. 先读现有 `docs/provider-snapshots/xiyou-tools-list-20260902.md`，列“已知工具/已验证字段/尚需确认字段”。已有发现结果不重复刷tools/list。
2. 对排名/格局/历史选对应接口，按已验证schema建立独立适配器；必要新schema仅做有预算的最小只读样本。不能推测分页参数、位置码、单位和日期意义。
3. 按明确关键词范围采集：用户主要词优先；报表小样写清selected/unselected和原因。预算不足不把未请求写成未返回。
4. 保留自然位/广告位、页面/位置、ASIN、站点、周期、采样时间；同口径自然位筛选、去自身去重后取最多3个。
5. 排名和份额分别传入market_merge→总表→模块；份额结构需支持同词多个ASIN、每家指标的分母语义与样本范围。
6. 指定竞对优先；推荐名单另存方法、覆盖关键词和来源。不可用自然前三冒充点击前三。
7. 市场扩词用独立来源标记ad_only/market_only/both；无广告历史用null，不污染原广告对账。

**通过标准**：假的真实schema响应里有位次时，最终私有bundle里同词同ASIN同值；改变primary实际改变查询参数；不同指定竞对采集对象不同；跨周期不混算；一条market_only仍出现在适当业务视图而广告对账不变。真正供应商无结果有响应/日期/参数hash证据，不能靠“我没调用”判定。

### R05：Sorftime图组被旧值遮挡，档案完整性与调用计数失真

**定位**：`worker/providers/sorftime_adapter.py:24/64/165/178`、`worker/competitors/profile.py:20/39/89`、`worker/pipeline/task_runner.py:172`。

**复现P02/P03/P06**：非空旧image_urls阻止完整新图组进入；自己只有ASIN仍ready；合成业务失败体有data时归一化仍接受。

**实施步骤**

1. 不要全字段统一“旧非空就赢”。按字段制定来源优先级：主图与完整gallery分开，保留主图来源与有序完整gallery，使用稳定source_image_id，不因排序改编号。
2. 对两来源差异记录各自时间/原值/选择理由；不能把新图组和旧sampled_at组合成假同日快照。
3. 对照真实产品schema补映射完整图组、五点/属性、上架日期、BSR/类目ID、视频/A+状态等。未验证大小写字段只能列待核实，不能猜“Photo一定有”。
4. `_content_json`/transport验证MCP `isError` 和已确认业务成功判据，错误体即使有data也不得作为成功事实。P03的code500是防御性合成案例，不是当前供应商schema证明。
5. 每ASIN记录success/no_result/schema_error/budget_exhausted等，不要except后无原因继续。计数同时包括失败尝试和成功数。
6. profile投影保留source_refs、真实sampled_at、调用汇总和字段缺失原因；当前build_competitor_profile把provider_calls重置为0，应修正。
7. module_status检查自己与竞对的必要字段；有字段值0（如0评论）不等于missing，空列表的业务含义按schema定义。不应因计划允许真实不足3家而永久要求3家ready。

**通过标准**：旧1图+新3图可查询完整3图及旧主图来源；自己缺资料必须partial；实际调用数在adapter、pipeline、bundle一致；输入错误被拒；缺失字段与当前选中值一致。

### R06：类目特征、文本比较、买家清单仍是孤立函数

**定位**：`worker/competitors/category_features.py`、`worker/competitors/text_evidence.py:27`、`worker/diagnostics/buyer_checklist.py:9`、`worker/diagnostics/listing_checklist.py:14`、`worker/pipeline/task_runner.py`、`frontend/report/competitors.js`。

**原因**：文本证据矩阵和buyer_checklist有本地函数，但生产pipeline没有调用它们；listing仍使用固定4个通用检查项。category-features虽然进入bundle，competitors.js没有加载该模块并渲染特征差距。

**实施步骤**

1. 把类目原始响应脱敏留底、标准化特征、适用/剔除/产品缺口草案、确认版本串成产物链。
2. 以已确认特征ID顺序，对自己和每家竞对的真实五点/属性正文提取原句及位置；缺正文是source_missing，不能判未提及。
3. 调用/复用text_evidence校验，英文原句与中文解释分开；错误feature_id/引用不匹配必须拒绝或有界重试失败批次。
4. 原句匹配支持“文本明确表达”，不是“功能实测属实”。无产品事实不能把建议包装成已具备能力。
5. 使用核心词意图、类目特征、真实评论或用户提供摘要形成通常5–7个买家确认要素；不能凭空编差评高频，证据不足少于5项应标不足而不是填充凑数。
6. 读取R03确认记录，冻结checklist；传给视觉链替代固定四问。主图质量四观察可以单列，不冒充买家清单。
7. 原有text/brief辅助函数必须校验输入输出；必要时增强而不是直接调用就宣布完成。

**通过标准**：生产路径的spy证明文本比较和买家清单被执行；feature×ASIN矩阵有可回查原句；缺正文/未确认/适用但没功能分别显示；清单版本在R07和bundle一致。

### R07：视觉模型未接生产，unknown观察也能被标ready

**定位**：`scripts/run_production_worker.py`、`worker/providers/xiyou_live_enrichment.py`、`worker/diagnostics/visual_evidence.py:14`、`listing_checklist.py:88/106`、`visual_brief.py`、`frontend/report/listing.js`。

**已证实**：生产返回值没有visual_evidence/checklist_evaluations/comparisons/briefs的实际生成。P05仅unknown/low且没有区域/来源的记录，可以使visual_evidence.status=ready；这说明当前ready只覆盖排列集合，不代表可判断证据。父级listing还有其他门槛，不能夸大为任意观察就使整个listing ready。

**实施步骤**

1. 由配置选择已验证支持图片输入的视觉型号，建立独立adapter，并在生产编排中显式调用。只读本地密钥，不写前端。
2. 同批请求为每张图带固定标签+image_url+稳定ID，记录expected IDs、模型/prompt/checklist版本、输入hash、预算；无图不请求，无确认清单则只做不依赖确认的观察。
3. 校验image_id/element_id属于本次范围，枚举/类型/区域/来源/可见文字有效。重复/错图/缺图可局部有界重试；read failure保持unknown。
4. 分开请求覆盖和可判断覆盖：observed不等于judged；unknown、低置信、无法读文字不能进入强弱排名。不能只给每个组合造unknown来拿ready。
5. 以版本化规则从已验证观察生成到位/弱/未确认/不可判定；同等级并列。调色证据不能推出调光。模型不返回最终运营动作。
6. 只有完整检查过的图组才允许“本图组未确认”；失败/缺页不能写产品无该功能。
7. 自有证据缓存复用，每次一家竞品对照，生成reference ASIN/image、target self image、借鉴方式和具体改法；逐图brief必须绑定已确认产品事实和规范约束。

**通过标准**：真实生产链至少验证2个不同ASIN图片；7张只读到6张为partial；unknown低置信不排名；同级并列；错ID拒绝；相同图组/清单/模型输入复用缓存；prompt中的外部图文指令作为数据处理。

### R08：否词误伤保护、负覆盖数与复制交互

**定位**：`worker/report/modules.py:178/252/278`、`frontend/report/negative.js:35/63/104`、`worker/tests/test_modules.py`。

**P07根因**：protected_terms从protected_converted/cautious/pending三个分组收集，排除了low_cvr_high_spend，所以相关/未知词进入诊断组后失去词组保护。
**P08根因**：classified_rows用总数减unknown再减missing，同一行被扣两次。
**其他缺口**：高花费分位写死0.75；有订单的行先continue，无法同时出现在低CVR诊断；无词根统计、无精准/词组两段纯文本复制、无确认版本持久化。前端只有JSON导出。

**实施步骤**

1. 分类前从完整原始事实构建保护集，至少含已转化/相关/相关性未知/缺少关键证据的词；分类只影响展示，不影响保护身份。
2. 每个词组候选对完整保护集匹配；文本归一化和分词规则版本化。保护冲突清单随候选持久化，确认不可静默绕过冲突。
3. 统计合格唯一记录，而不是减两类错误数；记录ID与归一化keyword处理规则清楚。0≤classified≤total，诊断重叠不多计。
4. 低CVR高花费是独立诊断标签，可与“已有订单保护”并存；不得因能诊断就允许否定。
5. 把分位值等新增业务参数放入配置schema/UI/版本；默认草案需符合既有授权，不能擅改当前策略。
6. 英文词根先定义为单词统计，命中至少两条不同候选；显示已转化/相关冲突，默认不直接复制；其他语言单列策略。
7. 一个共享资格函数驱动导出JSON、精准纯文本、词组纯文本、复制数量；两种匹配类型分开，一行一词。需要确认的动作读取R03持久确认范围，刷新后依然有效。
8. 前端复核保护集合包含所有分组；重复或畸形产物按fail-closed处理。空报告校验先于任何data[key]赋值。

**通过标准**：P07 phrase候选被拦，P08为0/1；已有订单低CVR可被诊断但不可否定；改变筛选不改变资格；刷新保持确认；复制数量=文本行数=相同范围JSON合格数。

### R09：实体有独立事实，却仍复制关键词动作

**定位**：`worker/ingestion/ad_report_parser.py:261`、`worker/diagnostics/optimization.py:39/65/142`、`worker/rule_engine/engine.py`。

**P01根因**：解析器已经计算实体原始量和比例，但_entity_contexts仅保留5个原始量，丢掉比例；_entity_diagnosis直接复用keyword的action/conclusion/rule_hits/next_action。两个实体指标不同仍同一个加投动作。

**实施步骤**

1. 保留实体稳定身份和独立CTR/CPC/CVR/ACOS/ROAS；若重新计算则严格从实体原始量算，零分母null，不读keyword总比例。
2. 对每实体调用适用的确定性规则评估；scope=keyword与scope=entity分开，明确共享市场事实的站点/关键词/周期条件。
3. 未覆盖实体规则输出not_judged及原因；不能因为有campaign/target名字就judged，不能用“仅建议”掩盖错误动作。
4. 实体判断输出自己的reason_facts、rule_hits、配置引用、动作。动作不可由AI改变。
5. entity_status不能用any ready代替全部必要实体满足；缺少实体或部分输入身份不完整时显示覆盖范围。

**通过标准**：A(spend10/orders10/sales100)和B(spend100/orders0/sales0)具有正确不同规则路径；B命中配置止损不能scale_up；实体五项和与关键词五项对账；改变A数据不直接改变B动作。

### R10：主规则漏用防守上限，未覆盖组合被当作保持健康

**定位**：`worker/rule_engine/engine.py:62/68/83`、`rules/defaults/*.json`、`worker/config/config_merge.py`、`worker/diagnostics/optimization.py:109/127/171`、`frontend/report/optimization.js:109/125`、策略UI/SQL验证。

**P10/P11**：ACOS=1000%、无market_opportunity_score时落到hold_steady；防守配置上限0.1、通用tolerance0.3、实际ACOS0.2仍defend_rank。当前分支使用通用tolerance，没有使用max_defense_acos。
**退出契约**：后端仅从输入row读取exit_condition，build_optimization_plan顶层不产生前端读取的observation_window/exit_conditions；optimization_module_status未检查退出条件，P01退出pending仍ready。

**实施步骤**

1. 列当前规则决策表：已覆盖、缺数据、规则冲突、未覆盖；保持原五色/灰色映射的兼容，不在代码里偷偷改商业门槛。
2. 防守执行已配置防守成本上限，CPC/利润/产品阶段约束按既有计划读取；参数确实缺少时记录待确认，不拿通用tolerance替代专用上限。
3. 无高优先规则命中不代表健康，使用显式待判断/待补范围。高ACOS但缺市场数据不能默认建议保持。
4. 定义统一monitoring结构：窗口、指标、算子、阈值、回撤动作、rule/config版本、适用scope。禁止模型随口填写阈值。
5. 后端输出与前端按同一schema读取；旧包映射层提供兼容，新包缺必要退出条件不得ready。
6. 配置引用必须列出规则实际使用的参数。修复前防守分支使用acos.tolerance，而配置追溯可以展示organic_defense；修复后保证“事实→命中条件→实际参数路径”一致。逐项核对策略UI、SQL白名单和默认模板，不要删除本来存在的organic_defense配置键。

**通过标准**：P10不再健康保持；P11不再绕过防守上限；P01缺退出不ready；观察到期/越阈值/缺快照/跨周期都有可执行的规则结果；同冻结输入两次动作和排序相同。

### R11：未知分母被认作有效份额，类目单位靠数值猜

**定位**：`worker/report/modules.py:32/71/94`、`worker/competitors/category_features.py:16/31`、`worker/providers/market_merge.py`、`worker/tests/test_modules.py`、`frontend/report/rank.js`。

**P09**：两个ratio有值但ASIN、期间、scope和分母全缺，share_module_status仍ready。现有test_zero_or_unknown_denominator_does_not_count_as_share反而断言这种rows应ready；这是测试固化错误口径。
**P04**：_share(0.5)算50%，_share(1)算1%；单位按值大小切换，数值边界不连续。

**实施步骤**

1. 明确ratio/percent_number/percent_text单位；每个Provider字段在有版本的schema中声明，无法确认单位先unknown_unit。不要按“大于1就是百分数”猜。
2. 区分“供应商直接给出且定义已验证的比例”与“本地除法”。前者可无原始分母数值，但必须有可核查的分母定义、期间、站点、ASIN和来源；后者必须有同口径数值分母且>0。
3. 无这些证据时unknown_denominator/scope_unknown；不能要求供应商一定返回分母数字，也不能完全跳过分母语义验证。
4. 不同行/不同ASIN只有同口径互斥才堆叠；采样比例标“样本内”，不冒充市场全量。未知覆盖不画“其余=100%-已知”。
5. 修正错误旧测试的期望，新增边界：ratio1=100%、percent_number1=1%、percent_text1%=1%；0.5同理；133.8%作为互斥份额应invalid，不截100%；类目不同特征总和可>100%。
6. 数值解析必须消费整个字符串，不能把“23.5garbage”正则前缀当合法数值；保留raw_value与unit供审计。

**通过标准**：P04/P09按schema和证据判断；分母0/未知/跨期不complete；UI状态/分母说明和后端一致；未知不显示0%。

### R12：缓存未接生产，进程预算耗尽后继续领取任务

**定位**：`worker/providers/cache.py`、`orchestrator.py`、`xiyou_live_enrichment.py:1/52/147`、`sorftime_adapter.py`、`scripts/run_production_worker.py`、`worker/runtime/production.py:466/598`、重跑RPC。

**源码事实**：有独立ProviderCache/Orchestrator，但生产路径直接使用无disk cache的XiyouLiveEnricher；真实rerun重新请求。西柚和Sorftime预算在进程启动时构造并跨任务共享，Sorftime5次额度可由一次4产品+1特征用尽。没有把预算耗尽作为领取前调度条件；后续可能取走任务后通用失败/部分降级。不能用自动重启服务绕过上限。

**实施步骤**

1. 将已有cache/orchestrator接入实际Provider链，先完成契约适配，避免复制另一套缓存实现。
2. 事实缓存键含provider/adapter/schema版本、站点、ASIN/词、周期、语义、授权数据范围；视觉缓存另含模型/prompt/checklist/图ID和URL列表hash。
3. 规则变更复用事实；图/核心词变更只使相关缓存失效；新run引用已有私有快照，不覆盖旧run。
4. 保留显式预算：请求数、credits、模型token分别统计。明确进程上限与任务上限，不改成无限制默认值。
5. 领取前判断本Worker是否具备继续处理任务的额度/能力；用已有状态机兼容方式暂停领取或明确转交/等待资源，不能反复领取再失败。不能用重启清零突破用户预算。
6. 有界退避重试计入预算；缓存损坏、未知费用、429/5xx、partial业务均有稳定原因；西柚5xx继续本地模拟，不索要不存在的测试接口。

**通过标准**：相同事实两次run仅第一次采集；只改规则零新增Provider/视觉请求；预算不足不发送超限请求，也不把持续队列误当完成；在fake进程测试连续6个任务验证耗尽路径和恢复策略。

### R13：运行证据临时目录清理后丢失，来源标记过强

**定位**：`worker/pipeline/task_runner.py:182/205/261`、`worker/runtime/production.py:419/533/564`、`worker/providers/xiyou_live_enrichment.py:119/219/276`、`worker/competitors/profile.py:111`。

**源码事实**：pipeline写provider-usage/rules-snapshot/run-meta等文件，但_bundle只读取6个模块+主表。TemporaryDirectory退出后这些独立证据没有上传；主表的配置版本号不能代替实际配置内容。竞对snapshot_version固定字符串，主market digest不覆盖竞对/图/确认结果。real_provider_verified是类属性，即使用Mock transport构造同类也为True。

**实施步骤**

1. 建立单一artifact registry，生成/打包/读取白名单由它或共享schema验证驱动，不维护三份易漂移列表。
2. 为持久主bundle加入manifest，或引用私有附件：冻结config/rules、provider用量、脱敏原始事实、确认对象hash、图证据、release manifest和各artifact SHA-256。
3. 临时目录退出前验证所有需要留底的文件已进入待上传内容；主bundle超过上限时使用受鉴权私有附件，不开放桶。
4. task/run/ASIN/站点/币种/时间/规则/配置/来源在每模块可解析到同一manifest。只带schema_version不够。
5. 来源证明由真实transport成功调用回执、请求/响应hash和配置的模式构成；测试Mock/injected不得因为类名而变成real_verified。不要只把布尔改True修报告标签。
6. 每Provider计数和失败原因保留；Sorftime profile投影不丢provider_calls；visual provider_calls不永远写0；actual/requested/estimated/credits分别说明。
7. 生产泛化异常保留可公开error_code和阶段，私有诊断关联请求ID/hash；不输出含key的URL、原始异常体或认证头。

**通过标准**：本地临时目录删除后仅凭私有报告及manifest能重现规则决策、定位来源和确认；篡改附件hash拒绝；Mock不能real；不同竞对事实生成不同快照hash；各模块调用数与真实预算回执对账。

### R14：页面缺少计划中的业务交互，不能只换配色验收

**定位**：`frontend/report/shared.js`、各模块HTML/JS、`frontend/styles.css`。当前9个抽查文件已证实线上等于本地。

**保留**：六个独立页面、共享导航、task/run参数、登录门禁、固定列宽与横向滚动已有基础，不合并成一个大HTML，不用iframe包全部页面。

**逐页完成项**

| 页面 | 当前缺口 | 修复后的业务表现 |
|---|---|---|
| 01总表 | 主要规则/市场缺口影响判断 | 先完成R04/R10；广告对账和market_only区分，未覆盖判断明确 |
| 02排名 | 份额只有自己一行的标量表，无每词多ASIN份额格局 | 有可比份额再画条，区分份额领先者与自然位标杆；来源/周期/采样说明 |
| 03否词 | 仅JSON导出 | R08的词根冲突、确认、精准/词组两段纯文本复制 |
| 04竞对 | 仅基础档案横排，没category-features加载/特征原句矩阵/数值差距 | 自己与竞品卡＋基础差距＋同维度特征表达；可选一家对照 |
| 05图片 | 固定四问、观察对象JSON.stringify、brief仍长表 | 买家清单版本→多方矩阵含图/图号→单竞品对照→逐图brief卡 |
| 06优化 | 追溯与退出大量待补，实体动作错误 | 同源事实/规则/配置/实体动作/观察退出；止损优先视图 |

**实施步骤**

1. 先为R02/R03输入确认设计具体表单，再逐页接真实产物。缺数据展示哪个步骤/字段待补，不要一屏内部reason英文。
2. 复用CSS变量，按LUNU-04参考白卡、浅背景、自己青绿、风险红橙、未知灰。每个长文本列明确宽度，不均分所有列。
3. listing中的“当前未请求视觉模型”是硬编码，应按当次产物显示；对象字段渲染成证据/图号/来源，不直接JSON字符串。
4. 逐图brief左图右说明：已有表达、薄弱点、保留元素、构图、英文草案、引用、真实性约束。图片加载失败保留来源和重试入口，不制造视觉判断。
5. 六页main标题展示产品/关键词/阶段；UUID与内部版本折叠详情。供操作的按钮只在资格条件满足时启用。
6. 完整/partial/failed/not_generated/旧包/0结果六类状态都验证；旧包不能因为没有新字段崩溃。

**通过标准**：1440桌面与390手机截图；表格在自己的容器横滚，不导致整页溢出；从任一报告跳转保留同task/run；真实图片/原句/动作可交叉定位；没有“换色后就完成”的结论。

### R15：补真实行为Gate，修复后才能重新做业务终验

**定位**：`scripts/run_uat.py:162`、`supabase/test_task_business_inputs.py`、`worker/tests/*`、`frontend/tests/*`、`PROJECT_TASKS.json`。

**问题**：当前UAT固定20个Gate，SQL测试列表仅迁移/production_jobs/strategy_versions，未运行独立的test_task_business_inputs；该文件本身也只查SQL字符串。旧fixture测试允许未知分母ready，一些测试只看实体facts未看独立动作。表面全绿不能保护本轮反例。

**实施步骤**

1. 把附录A每个反例改成正式行为回归：先断言正确预期并确认旧代码失败，再修实现；错误旧测试同步纠正并在变更说明给出计划依据。
2. 增加生产工厂贯通测试，不只run_task注入字典：模拟claim带业务输入，经真实factory→fake transports→pipeline→私有bundle→前端模块读取得到一致数据。
3. 新确认RPC通过一次性数据库执行测试验证权限/不可变性；静态检查不能代替SQL运行。当前测试未具备数据库时标未验证，不记passed。
4. UAT增加业务端到端Gate和007/后续迁移行为Gate；固定Gate数量若增加，同步编排合同和预期，不能删掉旧安全检查凑数。
5. 子能力完整状态要有正例也有反例：已采样无结果可partial；实现未接必须使完整业务Gate失败；无充分证据不能只让布尔字段控制结果。
6. 本地通过后按第6节发布/实测，不为看到某个通过计数反复运行同一UAT。

**通过标准**：第6节每项有当前版本证据，所有P0/P1必需能力已实接或有用户明确接受的范围调整；无范围调整的漏实现保持未验收。仅Provider字段无结果可以保留partial，但该字段请求/解析路径应已验证。

## 4. 给 Lunu 的上下文管理和交接格式

### 4.1 每次只做一个可验证的小闭环

1. 读取当前短交接、监督器选中卡，以及本文对应小节；用 `rg -n '^### R0'` 或标题定位定向阅读。
2. 声明本小卡预期改动文件，通常控制在一个责任域；SQL/client/runtime全链卡可以分子卡，但最终必须有串联测试。
3. 使用 `--claim` 原子领取；当监督器自动续领后继续它选择的安全任务，不能自己反复重选已通过卡。
4. 保存失败反例结果→修改→定向测试→契约检查→证据和文档→提交已审查的项目改动。不要顺手提交密钥文件或用户无关工作。
5. 外部缺schema时先完成fake adapter/规则/UI测试与其余本地卡。真实模型结果和fake证据必须分开。
6. 上下文将满时先落盘交接，准确写下一条动作。新模型不需要回读几百万token历史，更不能看到旧PROJECT_COMPLETE就结束。

### 4.2 短交接文件模板（实施时建立，建议不超过80行）

```text
审计基线：f1e69e9；依据LUNU-05，旧完成结论已纠正
当前目标：六模块业务验收
当前任务ID：review-rXX-子步骤
执行状态：RUNNING / 合法阻塞态
本卡已改文件：...
已复现：Pxx；命令/断言/结果
已通过：测试名、证据路径、产物字段
未完成：具体链路/字段，不写“继续优化”
外部待验证：能力、范围、预算；已有授权与缺失项
当前业务确认版本：...
下一安全动作：监督器给出的任务与首条命令
不要重复：已经通过且输入未变化的验证
Git基线/当前提交：...
```

### 4.3 一张卡完成回执必须包含

- 当前代码/配置/规则输入指纹，不手填“已验证”绕过检查。
- 验收案例ID和实际断言；例如P07的phrase_export_eligible=false，而不是“跑过tests”。
- 测试命令及成功/失败/跳过数量。
- 产物字段路径、task/run和schema（涉及生产时）。
- 本卡仍未验证的外部部分。测试通过但外部未执行不能混在passed描述里。
- 下一安全任务。完成的是当前卡，不自动完成整个项目。

## 5. 附录A：最小反例复现代码

下面全为离线合成样本，无真实Provider/数据库调用。实施者可保存为临时审核脚本，或转成对应unittest。**print用于复现；修复回归必须改成正确预期assert。**

```python
import copy
import json
from worker.rule_engine.engine import load_default_config, evaluate_keyword
from worker.diagnostics.optimization import build_optimization_plan, optimization_module_status
from worker.report.modules import build_negative_keywords, negative_module_status, build_share_board, share_module_status
from worker.providers.sorftime_adapter import SorftimeCatalogAdapter, normalize_product_detail
from worker.competitors.category_features import _share
from worker.diagnostics.visual_evidence import build_visual_evidence
from worker.competitors.profile import build_competitor_profile, COMPARISON_FIELDS

cfg = load_default_config()

# P01：实体独立动作和退出门槛
entities = [
    dict(campaign_id='A', ad_group_id='GA', target_id='TA', match_type='EXACT',
         impressions=100, clicks=20, spend=10, orders=10, sales=100),
    dict(campaign_id='B', ad_group_id='GB', target_id='TB', match_type='EXACT',
         impressions=1000, clicks=100, spend=100, orders=0, sales=0),
]
actions = build_optimization_plan([dict(keyword='led light', action_group='scale_up',
    ui_conclusion='increase', rule_hits=['synthetic-keyword-rule'],
    next_action_text='increase budget', ad_entities=entities)], {})
print('P01', [d['recommended_action']['action_group'] for d in actions[0]['entity_diagnoses']],
      [d['exit_condition']['status'] for d in actions[0]['entity_diagnoses']],
      optimization_module_status(actions))

# P02：完整gallery不能被旧单图遮挡
class Fake:
    def call(self, tool, args):
        return {'content': [{'type': 'text', 'text': json.dumps({'data': {
            'asin': args.get('asin'), 'images': [f'https://images.example/{i}.jpg' for i in (1, 2, 3)]
        }})}]}
adapter = SorftimeCatalogAdapter(transport=Fake(), marketplace='US', max_calls=5)
profile = adapter.enrich_profile({'self_asin': 'B012345678', 'competitors': [],
    'self_product': {'asin': 'B012345678', 'image_urls': ['https://images.example/old.jpg']}})
print('P02', profile['self_product']['image_urls'])

# P03：合成失败envelope，后续按已确认真实schema扩展
error = {'content': [{'type': 'text', 'text': json.dumps({
    'code': 500, 'data': {'asin': 'B012345678', 'title': 'should not accept'}})}]}
try:
    print('P03 accepted', normalize_product_detail(error, asin='B012345678', marketplace='US')['title'])
except Exception as exc:
    print('P03 rejected', type(exc).__name__)

# P04：单位不能按数值大小切换
print('P04', _share(1), _share(.5), _share('1%'))

# P05：有记录不代表有可判断证据
visual = build_visual_evidence(image_ids=['i1'], expected_element_ids=['e1'], evidence_version='v1',
    observations=[dict(image_id='i1', element_id='e1', answer_mode='unknown',
                       prominence='unknown', legibility='unreadable', confidence='low')])
print('P05', visual['status'], visual['observations'])

# P06：自有商品缺字段必须影响ready
base = {key: 1 for key in COMPARISON_FIELDS}
base.update(brand='Brand', title='Product', main_image_url='https://images.example/a.jpg',
    image_urls=['https://images.example/a.jpg'], bullet_points=['fact'], variations=['variant'], currency_code='USD')
profile = build_competitor_profile(self_asin='B012345678', self_product={'asin': 'B012345678'},
    competitors=[dict(base, asin=s) for s in ['B098765432', 'B087654321', 'B076543210']])
print('P06', profile['module_status'], profile['self_product']['missing_fields'])

# P07：低CVR诊断不能解除相关保护
groups = build_negative_keywords([
    dict(keyword='rope lights', clicks=12, spend=3, orders=0, relevance='unrelated'),
    dict(keyword='indoor rope lights', clicks=30, spend=90, orders=0, relevance='related')], cfg)
print('P07', [(r['keyword'], r['export_eligible']) for r in groups['phrase_negative']],
      [r['keyword'] for r in groups['low_cvr_high_spend']])

# P08：单行多缺失只计一次
groups = build_negative_keywords([dict(keyword='missing', clicks=None, spend=1,
    orders=0, relevance='unknown')], cfg)
print('P08', negative_module_status(groups))

# P09：未知分母/范围不能完整
shares = build_share_board([dict(keyword='led', keyword_market_share=.2, asin_keyword_dependency=.05)])
print('P09', share_module_status(shares), shares)

# P10：高成本、缺市场机会字段，不能健康保持
row = dict(keyword='bad spend', impressions=10000, clicks=100, spend=100,
           orders=1, sales=10, ctr=.01, cvr=.01)
print('P10', evaluate_keyword(row, cfg)['action_group'])

# P11：专用防守上限必须生效
cfg2 = copy.deepcopy(cfg)
cfg2['acos']['tolerance'] = .3
cfg2['organic_defense']['max_defense_acos'] = .1
row.update(organic_rank=1, spend=20, sales=100)
print('P11', evaluate_keyword(row, cfg2)['action_group'])
```

审核基线实测输出关键值：P01=两个scale_up且退出pending但ready；P02=旧1图；P03=accepted；P04=1→1%而0.5→50%；P05=ready；P06=ready且自有缺12字段；P07=可导出；P08=−1/1；P09=ready；P10=hold_steady；P11=defend_rank。

## 6. 修复后最终验收矩阵

### 6.1 本地Gate

| Gate | 必须实际验证 | 失败时 |
|---|---|---|
| 输入 | UI→client→RPC→claim→factory→bundle业务字段逐值一致；旧任务兼容 | R02未完成 |
| 确认 | 任务归属、身份、hash、新版本、并发不可覆盖 | R03未完成 |
| 排名 | 源rank值及日期范围→自己的位次/最多3标杆；指定词实际查询 | R04未完成 |
| 否词 | P07/P08、订单保护、独立诊断标签、复制/导出一致 | R08未完成 |
| 实体/规则 | P01/P10/P11、实体对账、退出条件、重复运行稳定 | R09/R10未完成 |
| 数值 | P04/P09、边界单位、分母语义、跨期拒绝 | R11未完成 |
| 产品 | P02/P03/P06、原始来源、完整图组、不串任务 | R05未完成 |
| 图文证据 | 引用可回查、清单真确认、P05、错图/少图/并列规则 | R06/R07未完成 |
| 缓存/预算 | 同事实复用、版本失效、耗尽不超限、不无限重试 | R12未完成 |
| 留底 | 临时目录删除后仍能读取全部必要证据与hash | R13未完成 |
| UI | 六页各状态、旧包、task/run导航、移动横滚和图卡 | R14未完成 |
| 回归 | 原有UAT+新增业务Gate无失败，skip有具体范围说明 | 不得进入完整业务通过 |

### 6.2 线上验收步骤（实施阶段执行，本轮未执行）

1. 先确认所有本地P0修复和必要P1路径通过；记录准确代码提交、SQL版本、规则/模型/prompt版本。
2. 只读核对本项目Worker服务ExecStart、目录、解释器和关键文件hash；读取必要状态不打印env。检查预算状态，不能重启绕过预算。
3. 发布审核过的前端和Worker包；数据库新迁移先在可控环境通过权限/幂等检查再应用。记录真实release manifest。
4. 用已授权测试账号、已经确认输入创建新run，小样覆盖核心词、至少自己+2家竞对、已确认清单和可取得图组；先固定预算与调用清单。
5. 从正式网站登录读取这个新run，验证：
   - 01：广告展示/点击/花费/销售额/订单五项对账为零；市场词不污染对账。
   - 02：至少抽2个词的源位次、站点、时间、ASIN、差距；有份额则核查分母，无份额必须说明真实请求与缺失原因。
   - 03：抽2条候选回查原广告、相关性确认、冲突和复制资格；不得写回Amazon。
   - 04：抽自己的档案和至少2家竞对，核价格公式/BSR可比条件，并抽2条特征原句。
   - 05：至少2个不同ASIN图号→实际图片→观察→判断→改图brief能逐项追溯；不能仅展示图URL。
   - 06：抽3条动作，至少包含不同实体或不同规则路径，逐项核事实、实际使用阈值、观察窗口和退出/回撤。
6. 当前版本重新做未登录、过期会话、跨店账号、直接私有对象、篡改task/run/附件路径的拒绝测试；记录HTTP与业务码，不能只把任意400当所有权限测试都通过。
7. 1440px桌面与390px手机截图带地址栏，同task/run/release；无登录浏览器直达报告看不到数据。截图不能包含凭据、私有签名URL或用户密码。
8. 逐模块输出“能力已接通/本run观察数/可判断数/合法无结果/待业务确认”。只有必需能力通过或用户明确接受的范围变更，才可以关闭阶段目标。

### 6.3 不得用于绕过验收的做法

- 把module_status/full_report_complete/real_provider_verified直接改True。
- 将没有调用过的采集器写成“供应商不提供”；需要能力/响应/schema证据。
- 把所有广告词都要求有排名或每家竞对都必须有未知字段，以此无限调用；应区分实现完成和本run真实覆盖。
- 把负覆盖数直接max(0,x)掩盖重复扣数；应修计数集合。
- 把实体动作改成永远pending掩盖缺少独立规则；有充分事实的正例必须可判断。
- 删除旧安全测试、改测试断言迎合错误行为，或仅改完成文档。
- 反复重跑相同付费请求/重启服务清预算，或要求西柚提供不存在的5xx测试接口。

## 7. 可直接发给 Lunu 的启动消息

> 请实施项目根目录《LUNU-05-项目审核验收与逐项修复执行清单-20260911.md》。先读第0–1节和R01，纠正旧的46/46业务完成结论；保留已验证的基础链路。按R任务卡建立精确依赖和指纹，用现有监督器领取，先复现再修复。11个离线反例是回归基线，重点先修否词保护、实体错误动作和防守上限，再贯通业务输入、确认、Provider、图文判断和持久证据。每次只处理一个小闭环并更新短交接；不重复跑无变化Gate，不以partial、数组非空、HTTP200或通用UAT全绿冒充全部交付。修复后必须用新版本新run完成第6节验收，并输出每条证据。不要将Secret输出到聊天、前端、日志或Git。
