# Amazon-Keyword-Command-Center-YX 项目规则

## 项目范围

- 本目录是 Amazon 关键词作战系统项目，不是微信日报项目。
- 任务开始先读取 `MEMORY.md`、`PROJECT_MEMORY.md`、`PROJECT_PLAN.md` 和 `PROJECT_STATUS.md`。
- 只读取与当前任务直接相关的源码、规则、测试和文档；不要扫描其他项目。

## 连续执行策略

- 用户要求“继续执行”或未明确要求暂停时，Codex 必须持续推进当前目标。
- 完成一个子步骤后，自动检查下一个可执行步骤并继续，不要仅汇报“已完成”后停下。
- 默认自行完成常规的文件读取、代码修改、测试、修复、重跑和项目内进程重启。
- 只有以下情况才暂停并请求用户协助：
  1. 发现真实异常且经过定位/安全重试仍无法解决；
  2. 需要用户提供业务判断、外部凭据、登录、权限或第三方服务确认；
  3. 操作会删除重要数据、产生费用、改变密钥/权限或改变核心架构；
  4. 当前阶段 Gate 无法通过，且继续执行没有新的安全进展。
- 非阻塞的警告、测试失败和实现缺口应先自行诊断、修复并重新验证，不要直接停下来等待下一条指令。
- 每轮汇报应包含：已完成内容、当前验证结果、下一步动作；只要仍有安全可执行的下一步，就继续执行。

### 连续执行状态机（强制）

- `PROJECT_STATUS.md` 顶部必须维护 `execution_state`、`current_objective`、`next_safe_action`、`stop_reason`、`last_action_fingerprint` 和 `verified_gate_snapshot`。
- `PROJECT_TASKS.json` 是持久任务图，`scripts/project_supervisor.py` 是唯一下一任务选择器；`PROJECT_STATUS.md` 的 `next_safe_action` 必须与监督器选出的任务一致，不能靠临时聊天记忆决定是否继续。
- `execution_state: RUNNING` 时，只要 `next_safe_action` 指向安全本地工作，主 Agent 就必须执行该动作、更新状态并继续下一动作；不得发送最终回复或把单个命令、测试、UAT、文档同步、子 Agent 完成当作停止条件。
- 开始执行监督器选中的本地任务前，必须运行 `python scripts/project_supervisor.py --claim` 原子领取；它会在并发锁内写入 `running` 和当前输入指纹，不能只在聊天中声称已领取。
- 外部 Gate 永远不由普通 `--claim` 自动领取。用户明确授权且本地队列清空后，必须使用 `--claim-external <task-id> --authorization-json <非敏感回执>` 原子领取；成功仍用 `--complete`，失败或授权撤回用 `--block-external <task-id> --reason-json <事件回执>` 回到阻塞态。不得把 JWT、密码、私钥或 Secret 写入回执。
- 每完成一项任务，必须通过监督器的 `--complete` 和结构化验证回执原子写入 `verified_fingerprint`；该转换会在同一个锁内立即领取下一项安全本地任务。只有它返回合法终止态才允许结束执行链。
- 只有 `BLOCKED_EXTERNAL`、`BLOCKED_RISK`、`USER_STOPPED`、`PROJECT_COMPLETE` 是合法终止态。进入终止态前必须穷尽安全本地任务，并把具体原因写入 `stop_reason`；`BLOCKED_EXTERNAL` 不得标记为项目完成。
- 防重复以输入状态和验证证据为准：任务 ID、执行类型、依赖、验收条件、声明的源码/配置/计划输入和 `verified_gate_snapshot` 均纳入指纹；未变化时必须跳过该动作，任一变化时旧回执必须失效，不得重复运行同一测试或审计来伪装推进。
- `running` 任务的输入若在执行中变化，`--complete` 必须拒绝旧回执；主 Agent 运行 `--claim` 刷新领取指纹、重新执行受影响验证后再完成，不能让任务永久卡死或沿用旧证据。
- 已完成任务若 `input_fingerprint != verified_fingerprint` 或缺少验证回执，必须视为 stale 并重新开放；指纹一致且回执有效的任务不得重复执行。
- 若 `local_safe_queue: empty` 但 `execution_state: RUNNING`，这是状态错误；必须重新清点任务并修正为具体 `next_safe_action` 或合法终止态，不能安静停止。
- 心跳自动化只允许在 `RUNNING` 时保持激活；进入任一合法终止态后必须暂停，等待用户新指令或阻塞解除再恢复，禁止完成态空转消耗 token。

### 连续执行收尾协议

- “测试通过”只是一个子步骤完成，不是本轮任务完成条件。
- 每次实现或修复后，必须按顺序检查：源码缺口 → 相关测试 → 集成/UAT → 进程级或契约验证 → 文档状态 → 下一项安全工作。
- 如果下一项只是需要外部凭据，先完成所有不依赖凭据的本地模拟、契约、失败路径和文档工作；不得把外部阻塞提前当作整个阶段完成。
- 最终回复前必须明确列出：当前阶段仍有无本地可执行项、已验证的 Gate、唯一阻塞原因；若仍有本地可执行项，不得结束当前执行链。
- 继续执行时优先使用一个持续目标和任务计划，避免完成单个命令后丢失上下文。

### 执行器防早停清单（强制）

- 每次工具命令返回后，不得直接结束本轮；必须先重新读取当前计划和状态，判断是否存在未完成的本地任务。
- “命令成功”“测试全绿”“文档已同步”都只能关闭对应子步骤。只有以下两个条件同时满足，才允许结束本轮：
  1. 当前阶段的源码缺口、回归测试、集成/UAT、契约/进程验证和文档同步均已检查；
  2. 没有任何不需要用户输入、外部凭据、费用、权限或危险操作的下一步。
- 发现外部阻塞时，不得把整个项目标记为完成；必须保留未完成项、阻塞原因和已完成的本地替代验证，并继续执行其余安全工作。
- 运行 `scripts/run_worker_once.py` 时，必须继续检查是否需要 `scripts/run_worker_loop.py` 或其他阶段任务；单次任务成功不得作为连续开发完成信号。
- 最终回复前必须执行一次“状态重读 → 本地任务清点 → Gate 汇总 → 阻塞确认”；若清点结果仍有本地任务，继续执行而不是回复用户。
- 最终回复前还必须运行 `python scripts/project_supervisor.py`；若返回 `RUNNING`，必须运行 `python scripts/project_supervisor.py --claim` 并继续其 `next_task`，不得结束本轮。

### 主 Agent 与子 Agent 监督

- 主 Agent 以项目目标为唯一总目标，维护任务计划，按需分配代码、测试、安全、流程/文档或部署审计等安全本地子任务，并复核每个子 Agent 的改动和 Gate。
- 子 Agent 必须返回改动文件、验证命令与结果、未完成项和外部阻塞；子 Agent 完成不得被主 Agent 视为项目完成。
- 主 Agent 收到子 Agent 结果后必须重新读取计划/状态，继续执行源码缺口、测试、集成、契约/进程验证、文档同步和下一项安全工作。
- 外部阻塞只冻结对应外部 Gate，不冻结仍可本地执行的子任务；监督清单与静态检查见 `docs/acceptance/continuous-supervision.md` 和 `scripts/check_continuous_execution.py`。
- 子 Agent 完成并被主 Agent 复核后，必须关闭并释放 Agent 槽位；不得让已完成 Agent 长期占用并发额度，导致下一轮无法继续分配任务。

### 跨轮持续唤醒

- 用户明确要求持续推进整个项目时，主 Agent 必须先检查平台级持续目标：若 `get_goal` 返回空，调用 `create_goal` 一次并把目标绑定到完整项目目标；已有 active 目标时不得重复创建。单个回复结束、上下文压缩或子任务完成都不能把 active 持续目标视为完成。
- 平台级持续目标负责跨执行轮次保持“为什么继续”，`PROJECT_TASKS.json` 负责持久化“下一项做什么”，两者缺一不可。只有达到本文件允许的真实阻塞、用户停止或项目完整完成条件时，才能按平台规则将持续目标标记为 blocked/complete。
- 当前任务的心跳只在 `execution_state: RUNNING` 时激活并跨轮重新唤醒主 Agent；每次唤醒都按本文件重新读取状态、执行 `next_safe_action`、清点后续安全工作并按需分配子 Agent。
- 心跳只解决“最终回复后再次唤醒”的生命周期问题，不扩大安全授权；真实 Provider、部署、凭据、费用、权限和重要数据删除仍遵守安全边界。
- 心跳轮次若仅有某个外部 Gate 阻塞，必须继续检查其他本地工作；只有本地安全队列确实为空时，才进入 `BLOCKED_EXTERNAL`、记录阻塞并暂停自动化，等待外部状态变化。

## 任务入口

- `scripts/run_worker_once.py` 是单次任务执行器，不是常驻 Worker。
- 需要连续处理队列时，应使用或实现循环/监督入口；不得把单次执行成功当作整个阶段完成。
- 项目状态以用户最新指令、实际代码/测试结果和 `PROJECT_MEMORY.md` 为准。

## 安全边界

- 未经用户明确授权，不调用真实外部 Provider，不发送消息，不花费费用，不删除重要数据。
- 常规本地测试和修复无需逐步询问确认。
- 同一操作失败两次后，先定位根因；不要无限重复相同失败操作。
