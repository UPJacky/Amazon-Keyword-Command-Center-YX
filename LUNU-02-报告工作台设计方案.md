# Lunu 执行方案 02：蓝色报告工作台设计

日期：2026-09-07。仅设计方案，尚未修改 HTML/CSS。与 LUNU-01 配套；视觉完成不代表数据与判断逻辑完成。

## 1. 依据及现状

用户本轮参考图目录：`images/参考/1/`。

| 图片 | 使用重点 |
|---|---|
| FhxbQzRx58nWa3K5wc_rnPsoPKnF.png | 否词分组、实际阈值卡、理由、慎否保护 |
| FmR_xdNqclwe39GOKcbL1nP2J7-F.png | 一个商品一行、自己行高亮、产品缩略图与字段密度 |
| FqLIyDL5Y59m-v-xtD5FgncO6Qyu.png | 自己排名+三个标杆卡片的宽表 |
| FtvHFqakKI9kCpOiXPj3WhURlFts.png | 总表指标、分组筛选、分层表头与趋势 |
| FvS62HElGvN7M9XThrp1iHUfpYly.png | 每要素成对图片，下方三维判断、证据与建议 |
| FvWnbcu5ZcC4DuEOOVbaPTJuyvDd (1).png | 产品图与诊断内容关联；不沿用大段原始 Markdown 和巨大任务 UUID 标题 |

原 DESIGN-airtable.md 是营销页设计分析，黑色 CTA、较大展示字体和营销段距不适合密集运营表格。现有 styles.css 已用蓝色，并非完全没改配色；主要问题是内容层级和结构：侧栏缺实际 ASIN、模块顶部技术说明过多、rank 缺三标杆、否词追溯列 600px、optimization 事实列 700px、普遍把工程元数据直接铺在主工作区。

本方案在报告工作台范围优先于 DESIGN-airtable.md 的视觉项，保留原文作为参考。业务色与判断规则仍以版本化 action_mapping/策略为准。

## 2. 统一尺寸与颜色（执行时采用，不再临场发挥）

```css
/* 合并到现有 :root，复用变量，避免文件末尾重复覆盖同一选择器 */
--canvas: #eff3f9;
--surface: #ffffff;
--ink: #172b4d;
--muted: #63738c;
--line: #dbe4f0;
--accent: #2458d9;
--accent-active: #1d46ae;
--navy: #183d7a;
--hero: linear-gradient(110deg, #193e7e, #2455d7);
--shadow: 0 6px 20px rgba(31, 58, 100, .05);
```

- 桌面外边距 20px；侧栏 220px，间距 20px；主区 min-width:0，不设整页超宽最小宽度。
- 侧栏白底、12px 圆角、1px 边框；导航高 46px，选中浅蓝底+蓝色左线+aria-current，标签保持中文。
- 顶栏 56px：品牌、返回工具、退出。主标题用模块中文名，不用任务 UUID。
- 模块蓝色标题区 104–120px，padding 22px 26px，h1 28px/1.3/700；下方一行 ASIN、站点、采集/报告时间、覆盖状态。技术来源放折叠区。
- 指标卡默认四列，间距12px，高88–96px；标题12px、数字26px；总表可八列（大屏），1440px 时四列，手机两列。
- 正文14px、表格13px、行高1.5；使用系统中文字体，不为字体添加外部网络依赖。
- 卡片圆角12px；控件高36px；输入/按钮具备键盘焦点。浅绿仅说明已验证事实，黄/红用于需注意事项，不给每段说明都上彩色背景。

## 3. 共用页面顺序

顶栏 → 左侧模块导航 / 右侧业务标题 → 指标卡 → 一条必要的数据覆盖提示 → 搜索/筛选/导出 → 主要结果 → 可折叠“判断规则与数据来源”。

普通用户第一屏看到“当前结论和依据”。MODULE 02、schema_version、JSON 文件名、哈希、Provider 技术状态进折叠详情；保留可追溯性，但不让用户阅读工程日志才能理解报告。

数据覆盖条写具体数量，如“91 个词，已采集 20 个，18 个可比较”；未采集不能写成“0 个有排名”。非敏感错误可给“本次运行未生成图片分析”，不要把所有错误都变为“确认登录权限”。

## 4. 六页布局

### 总表

指标：关键词数、曝光、点击/CTR、订单/CVR、花费、销售额、ACOS、市场覆盖。市场总量只有可比周期且有数据才计算，明确覆盖词数，不能混周/月。

筛选沿用项目五色业务组及灰色缺数，不照搬截图的十种结论枚举。表头分组为关键词/市场/竞对/自己/广告/行动。首列词+结论标签；趋势只在有真实序列时画 SVG 小图，缺历史显示“暂无历史”。点击行展开事实、规则、参数及来源。

### 自然位标杆

四卡：关键词数、自己已观测排名、三标杆齐全、待补采。主表：关键词、搜索量及周期、自己自然位、标杆1、标杆2、标杆3、差距。标杆格包含 64px 图片、ASIN、两行标题、位次、简要价格/评分；完整来源可展开。

自己缺排名和竞品不足分别展示；不能用三个空白框冒充三标杆。历史变化放二级展开区，先让用户看懂本次横向对比。

### 否定词清单

四卡：零单候选、低 CVR 高花费、需相关性复核、可复制数量（去重）。下方浅黄阈值条展示实际生效参数，不出现“示例阈值”充当生产规则。

按筛查原因分区，表列：词、点击、花费、订单、CVR、建议、一句话理由。行展开显示完整追溯。慎否/待确认不出现可复制按钮；精准/词组复制分开。后端判断和导出资格按第一份方案。

### 竞对对比

一个商品一行，自己固定首行浅蓝背景。列：商品（图/ASIN/标题）、价格、评分、评分数、近30天订单估计、BSR、变体、内容资产、图片数。原始来源未提供的字段显示“未返回”，布尔未知不用“无”。

少于所需竞对数提示覆盖不足；不显示空的“已完成竞对分析”。商品图片按真实 URL 加载，失败占位显示“图片无法加载”，保留标题和 ASIN。

### 图片与卖点

按要素分区：商品形态、尺寸、功能、场景、卖点证据。每区先显示“我的图/竞品图”配对，再显示确认速度、证据可靠性、表达清晰度三维结果。每条结论引用图序、观察事实和建议；未知维度展示待核验。

桌面每行最多两个竞对配对，保持图可读；大于1920px 可配置四对，禁止强制把八张图挤入窄屏。图片 object-fit:contain，白底，单图高180–220px。查看原图通过受控交互，不展示原始私有柜链接。长分析文本转为结构化段落，优先 textContent，不能直接把模型 HTML 注入 DOM。

### 广告优化

指标：建议数、优先处理数、观察中、数据前提不足。行动表：关键词/广告对象、建议、关键数字、理由、观察与退出条件。事实/规则详情折叠；不保留700px工程事实长列作为主体。

只有聚合词数据时明确“关键词级建议”；匹配类型冲突/预算迁移板块需要实体明细才显示实际判断。

## 5. 表格与响应式硬要求

- table-layout:fixed + colgroup；表头居中，内容左对齐。数字及 ASIN 不换行；标题/理由自然换行，避免全局 overflow-wrap:anywhere 拆散所有内容。
- 只有 .table-scroll 横向滚动，主页面不能被撑宽。表格第一列 sticky，表头 sticky 时验证滚动容器层级与遮挡。
- 建议列宽：关键词200、数字90–110、自己排名140、标杆300、商品260、理由320；按实际列和计算总宽，不套所有页面同一个 min-width。
- >=1280px 侧栏常驻；768–1279px 顶部模块导航/抽屉；<768px 单列主区、指标两列、筛选横向滚动、表格保留原宽横滚。
- 390px 页面不得横向溢出；触控目标至少44px。1440x900 首屏能看到至少一行实际结果，不能被标题和技术说明占满。
- 加载用骨架屏；无记录、筛选无匹配、未生成、部分数据、权限失败各有准确文案。错误时不显示虚假的0指标。

## 6. 修改文件边界

1. `frontend/styles.css`：合并现有样式，清理同一报告选择器的冲突规则。
2. `frontend/report/shared.js`：业务上下文侧栏、统一状态卡、带列宽的 table helper、可折叠来源。
3. `frontend/report/index.html`、rank/negative/competitors/listing/optimization.html：独立页面保留，统一标题/指标/内容区语义。
4. `frontend/report.js` 与五个模块 JS：各自业务渲染；共享纯展示组件，不复制鉴权。
5. 如新增共用文件，同步 `scripts/build_pages_demo.py` / `scripts/build_pages_live.py` 白名单及引用检查。
6. 工具页仅补充状态与报告上下文所需项；登录页可沿用当前蓝色体系，不扩大到全站架构重写。

本方案不要求新建 React/Vue、不引入 iframe、不用 CDN UI 库。模块拆文件便于维护，真实访问权限仍由服务端负责。

## 7. 验收与省 token 执行方法

- Lunu 先做一个共享壳和自然位页，验证1440与390布局，再将规范应用其他模块；不连续多轮口头调色。
- 每模块在真实/部分/无数据三种状态检查：首屏信息、表格布局、筛选、来源折叠、缺数文案。
- 最后对已修复的新 run 逐页截图检查，核对正文真实字段与后端一致；截图数据不能写进代码作为演示补丁。
- 看对比图的结构、色彩、密度，不复制品牌、商品数值和未经确认阈值。
- 每个可用版本一个 commit；纯 CSS 调整跑前端相关验证和页面检查即可，不重复完整 Worker UAT。数据/规则集成结束再跑完整验收。
- 不再以“六个 HTML 都打开了”验收，必须同时有正确数据、可解释判断、可读布局和门禁。

给 Lunu 的简短指令：先执行 LUNU-01 的数据与规则任务，再按本文件逐页优化。每次只读当前模块与共用文件，完成后写简短进度和下一个任务。原有任务图“全部完成”不能覆盖本文已证实的缺口。
