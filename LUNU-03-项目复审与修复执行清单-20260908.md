# 项目复审与修复执行清单（交给 Lunu）

审查日期：2026-09-08。审查基线：本地 Git `57bcbdc`。本轮只读取源码、计划和历史验收记录，在临时目录运行无网络复现，并新增本文；未修改业务代码、数据库、服务器、发布包、目标或任务台账。

## 1. 首要结论

**当前不能宣布六模块项目完成。此前“26/26 完成”的回复需要纠正。**

`PROJECT_STATUS.md` 的完成结论与实际源码、`MEMORY.md`、`PROJECT_MEMORY.md` 和 LUNU-01 的业务要求冲突。任务执行成功、缺失提示正确、权限测试通过，都不能替代排名、竞对、视觉分析和广告实体判断的交付。

当前已经有可复用的基础：独立页面、私有报告读取、任务流水线、广告聚合、三标杆构建器、竞对字段结构和蓝色工作台。不要推倒重写。主要问题集中在生产采集接线、产物打包、事实粒度、判定完整性和验收证据。

### 本轮证据边界

- 已核对本地生产 CLI、runtime、pipeline、Provider、模块生成器、前端模块、解析器和任务监督器。
- 已运行下文列出的无网络复现；临时产物在退出后清理。
- 没有重新登录线上站点或服务器，没有新增真实 Provider 调用；线上部署状态只能引用既有记录，不能当作 9 月 8 日实测。
- 本文不是全面渗透测试，也没有重新证明现网 RLS、安全配置和所有历史测试仍然通过。
- 不读取或抄写密钥文件；交接材料不得包含 token、密码、私钥、完整签名 URL。

## 2. 优先级和执行顺序

P1：影响核心功能、业务判断或完成结论，发布验收前必须解决。P2：追溯、统计、配置和易用性缺陷，随本轮整改完成。

| ID | 优先级 | 问题 | 证据 |
|---|---|---|---|
| R01 | P1 | 完成判定把“未生成”当成业务完成 | 计划、台账、源码直接冲突 |
| R02 | P1 | 生产入口未接完整市场、排名及按任务竞对采集 | 源码确认 |
| R03 | P1 | 图片诊断生成后被生产打包漏掉，视觉判断也未贯通 | 源码＋局部复现 |
| R04 | P1 | 广告实体共用关键词总额和同一个动作 | 已复现 |
| R05 | P1 | 词组否定漏保护诊断组中的相关词 | 已复现 |
| R06 | P1 | 相关性没有完整的生产输入和传递通路 | 源码确认 |
| R07 | P1 | 线上验收记录与当前源码输出不一致 | 历史证据对比，部署原因待核实 |
| R08 | P2 | 观察窗口、退出条件、复盘和规则版本未输出 | 已复现＋前后端契约对比 |
| R09 | P2 | 生产临时目录中的快照、用量未完整持久化 | 源码确认 |
| R10 | P2 | 否词覆盖计数为负，高花费阈值硬编码，低 CVR 分组范围有歧义 | 部分已复现，其余源码确认 |

建议顺序：R01 → R07 只读部署基线 → R04/R05/R10 → R06 → R02 → R03 → R08/R09 → 集成测试 → 新发布、新 run 验收 → 最后更新完成状态。

本轮用户只授权审查和方案。收到后续实施指令后再修改代码、领取任务、部署或进行真实调用；不要因文档里出现执行步骤就自动执行。

## 3. R01：纠正完成标准，避免再次错误关闭目标

### 位置与证据

- `PROJECT_STATUS.md:7` 附近记录 `PROJECT_COMPLETE`，同时下方记录竞对/图片未生成，排名为零覆盖。
- `PROJECT_TASKS.json` 的 `module-gap-p8-live-release` 允许“真实或明确 partial 数据”，其通过回执甚至包含两个 not_generated 模块。
- `scripts/run_production_worker.py:main` 与 `worker/runtime/production.py:ProductionWorker._bundle` 固定输出 `full_report_complete=False`。
- `scripts/project_supervisor.py:_verification_errors` 主要检查回执字符串与 result=passed；不会核验回执是否真的满足业务要求。
- LUNU-01 明确指出不能用空数据或 HTTP 200 替代完成，但 9 月 7 日收尾又重复了这一错误。

### 修复步骤

1. 在实施开始时统一 MEMORY、PROJECT_MEMORY、PROJECT_PLAN、PROJECT_STATUS，保留历史记录并注明旧完成结论失效，不能删除旧证据。
2. 将任务图补为本文实际缺口，不要仅修改状态文案。按项目监督器协议领取与完成任务。
3. 分开 `run.status=completed`、模块可用状态、业务覆盖率和整个项目验收。ad-only 可以成功完成一次运行，不能因此关闭六模块目标。
4. 定义一个结构化业务 Gate：每模块记录是否接入生产、必需字段、有效覆盖、来源、缺失原因、判断样本验证结果。
5. 合理的供应商空结果允许 partial，但必须证明采集已执行且响应确实为空；“代码没有调用”“生成器没接线”“打包漏文件”是实现失败。
6. 监督器的通过回执应引用实际存在的证据文件及其摘要/hash、代码版本、验收项 ID。至少对最终 Gate 校验关键数值与必需模块，不能只凭自然语言 passed。

### 验收

- 两个 not_generated＋其余 ad-only 的 fixture 必须使完整业务 Gate 失败。
- 供应商真实无结果、配额不足、采集失败分别可见，不能统称“缺数”。
- 原有鉴权和广告对账通过记录继续保留，不为修正完成结论而重复做无关测试。

## 4. R02：生产采集通路仍未补齐

### 位置与证据

- `scripts/run_production_worker.py:main`：仅 `--ad-only` 或 `--xiyou-keywords`（1–10 词）。
- `worker/providers/xiyou_live_enrichment.py:XiyouLiveEnricher`：仅 `get_keyword_info`，不采排名、趋势、竞对资料。
- `worker/runtime/production.py:_execute`：只有市场 enricher factory；竞对仍是构造时固定 `competitor_profile`，CLI 没有按任务创建档案。
- `worker/pipeline/task_runner.py:_provider_result` 严格只接受 market_rows、provider_snapshot_version、usage；不能直接往返回对象塞竞对/视觉数据，否则报结构错误。

三标杆过滤排序、MARKET_FIELDS 新增字段等本地改进确实存在，应复用；问题是缺少生产来源。

### 修复步骤

1. 保留 ad-only 明确降级模式，新增可运行的完整采集编排模式，不靠扩大关键词参数伪装完整功能。
2. 优先读取仓库已有工具 schema/样本，再对仍缺参数的工具做授权 discovery。不得凭名字猜参数、分页或响应结构。
3. 按 task/run、ASIN、站点、关键词和时间窗口采集；至少接通关键词指标、ABA/趋势、自然位自身与标杆、按任务选择的竞对及商品资料。
4. 新增版本化 enrichment 结果结构，兼容旧三字段返回；明确 market、rank、competitors、visual evidence 的边界，并同步所有消费者和测试。
5. 复用已有预算/缓存/重试组件，统一实际调用计数。线上当前 XiyouLiveEnricher 自身无缓存、无重试，不要把通用组件有测试误认为已接上真实路径。
6. 配额用尽或服务异常时保留可用广告报告并标记对应模块失败/部分完成；不得无限自动重试，也不要求西柚提供不存在的 5xx 测试接口。
7. 同一进程连续处理两个不同 ASIN，必须独立生成竞对档案，不能共享上一任务的产品内容。

### 验收

- 假 Provider 端到端测试：请求 → normalize → merge → module → production bundle，逐字段匹配源值。
- 有效三标杆、自身排除、重复 ASIN、空结果、分页、站点/周期不匹配、预算耗尽均覆盖。
- 周搜索量不可命名为月量；Top3 点击份额不可当自然位排名；未知排名保留 null。
- 真实样本取得数据来源和覆盖证据后才进行最终验收；不以 fixture 的成功替代现网采集。

## 5. R03：图片产物漏装包，视觉 ready 判定失真

### 位置与证据

- `worker/pipeline/task_runner.py:178` 生成 `listing-diagnostics.json`。
- `worker/runtime/production.py:400` 的打包枚举仅 rank-benchmark、negative-keywords、optimization-plan、competitors，漏 listing。
- 本轮用样例运行 run_task，listing 文件确实存在；生产 _bundle 仍不会读取它。
- `worker/diagnostics/listing_checklist.py:73` 只要自有图片有任意 observation 就标 ready；`:79` 调 build_checklist 时没有传 evaluations。
- 复现：自有一张图片加 `observations=[{"text":"red"}]`，竞品图为空，模块为 ready，但 checklist 四项都是 unknown，ai_status=not_requested。
- pipeline 把 image_urls 重建为简化图片对象，没有贯通实际视觉 observations/evaluations。

### 修复步骤

1. 将 listing 纳入生产模块注册表/白名单与打包循环，检查前端 readModule 能取到同一文件。
2. 用一处模块注册表驱动生成、打包和完整性检查，减少不同枚举漂移。
3. 设计视觉结果注入字段，保留 image_id、来源、采集时间、观察内容、模型版本、调用量、证据引用与 evaluations。
4. build_listing_diagnostics 显式接收 evaluations 并交给 checklist；禁止任意一条描述就升级 ready。
5. ready 需要本模块必需检查项完成且证据引用有效；部分图、部分分析使用 partial。证据引用必须指向当前任务真实图片，不能任意非空字符串即通过。
6. 图文结论依照证据呈现；供应商没返回或模型未分析，显示具体状态。图片配对保留竞对 ASIN，不跨竞品混合结论。

### 验收

- 即使不调用视觉服务，生产 bundle 也含诚实的 partial 图片模块，而不是漏文件。
- observations 有内容但无 evaluations 时不为 ready。
- 完整测试证据到 checklist 的 pass/fail、图片对比和前端展示逐值贯通；无图和坏引用不能给完整结论。

## 6. R04：广告实体只有名称，没有独立指标，却被判定为可执行

### 位置与复现

- `worker/ingestion/ad_report_parser.py:180–265` 按 keyword 累加数字，ad_entities 只保留去重身份字段。
- `worker/diagnostics/optimization.py:_entity_diagnosis` 在身份字段齐全时直接复制关键词 action；`:109` 为每个实体写同一 `facts=dict(data_facts)`。
- 构造关键词 spend=110、orders=10，包含 A/Exact 和 B/Broad 两个实体；实际输出 A 和 B 都是 spend=110、judged，并继承相同动作。

这会把本来一个盈利、一个亏损的活动误判成同样表现。补齐活动 ID 不能解决指标粒度问题。

### 修复步骤

1. 解析时同时保留关键词总表聚合和实体粒度聚合；实体键包含活动、广告组、投放目标、匹配类型、必要的广告类型/周期。
2. 每个实体累加自身 impressions/clicks/spend/orders/sales，并独立重算比率。关键词总表仍使用原口径。
3. 实体判断基于实体事实和配置独立计算，不复制关键词总表动作。身份齐全但指标/观察窗口缺失仍为不可判定。
4. 若没有预算、投放位或历史观察数据，不输出相应预算迁移/结构内耗确定性结论。
5. 混合完整和不完整实体时，整体状态使用 partial，不能因 any(ready) 报全部 ready。

### 验收

- A 花费10/订单10、B 花费100/订单0，同词总花费110；两实体显示10和100，结论各自计算。
- 实体指标相加必须回到关键词聚合，再与原报表对账。
- 只有身份字段无实体指标时，judgement.status 不得为 judged。

## 7. R05：词组否定可能误伤相关词

### 位置与复现

`worker/report/modules.py:149` 先将高花费低 CVR 词放诊断组；`:166` 的 protected_terms 只收 protected_converted、cautious、pending，漏了诊断组。

在默认配置下输入：

| keyword | clicks | spend | orders | relevance |
|---|---:|---:|---:|---|
| rope lights | 20 | 1 | 0 | unrelated |
| indoor rope lights | 20 | 100 | 0 | related |

实际输出：rope lights 位于 phrase_negative，export_eligible=true；indoor rope lights 位于 low_cvr_high_spend。词组范围覆盖已确认相关的长词，却没被拦截。

另外 `frontend/report/negative.js:38` 的保护集合也未包含正常的 protected_converted/low_cvr_high_spend 行；对异常或旧产物的重复、冲突防线不完整。

### 修复步骤与验收

1. 从全部原始候选事实建立保护词集合，再进行互斥 UI 分组；保护资格不依赖最终放在哪个卡片。
2. 相关、未知、已有订单、数据不全等词的保护规则必须明确；诊断组不得解除保护。
3. 冲突词组降为待确认并输出 conflict_keywords，export_eligible=false。
4. 统一大小写、Unicode、连续空白和词边界；不要用任意子串误伤其他单词。
5. 前端导出对所有桶做防御检查，包括同词多桶冲突；有词组冲突不得导出。
6. 上述两行样例必须阻止 rope lights 词组导出；加入已转化保护、unknown、重复桶、混合大小写和空白测试。

## 8. R06：生产相关性事实无法到达否词分类器

`build_negative_keywords` 依赖 relevance/relevance_source，但解析器没有相关性字段，`market_merge.py` 的白名单也不转发相关性事实；当前 enricher 没有提供产品功能/场景确认通路。

本轮直接跑仓库 LED 样例，结果为：pending_confirmation=28、protected_converted=63、phrase_negative=0、exact_negative=0。这个保守结果正确，但说明“可复核相关性”的生产功能尚未完成，不能通过给所有词默认 unrelated 来补数。

修复：新增按店铺/ASIN/任务绑定的产品功能与场景确认数据，定义版本、确认人、时间和证据来源；通过受控的版本化输入传到 pipeline。AI 推断只作为待核查建议，不能直接成为已确认 unrelated。用户确认、模型建议、未提供三种来源分开。同步输入验证、存储权限、快照和前端复核界面。

验收：同一词在 related/unrelated/unknown 下按规则产生不同结果；缺来源不得自动升级为可导出；不同 ASIN/店铺确认数据不串用。

## 9. R07：核对 Worker 部署版本，不能只看 Pages 提交

历史 `PROJECT_STATUS.md` 记录新 run 否词为24词组/4慎否；本轮当前源码在同一命名 fixture、默认配置、无相关性输入下输出28待确认/63订单保护。两者不一致。由于本轮未读现网输入 hash、配置和 Worker 文件，**不能断言一定是旧代码**；这是必须核实的部署/证据问题。

执行时只读检查：

1. 精确定位当前项目服务单元及 ExecStart、工作目录、Python、入口参数；敏感环境值不要打印。
2. 对生产关键文件计算 SHA-256，并与本地发布清单逐个比对：pipeline、modules、runtime、解析器、Provider、规则配置。
3. 读取目标 run 的 input_sha256、config_version、report schema、模块 schema、report_scope 与安全摘要。
4. 本地使用相同输入与配置复现；配置和输入都一致仍有差异才定位版本/代码。
5. 新增 release manifest：Worker 代码提交/文件摘要、Pages 版本、规则版本、构建时间。每个新 run 绑定 Worker release；Pages 新提交不证明服务器更新成功。
6. 发布只重启已经定位的本项目服务，禁止模糊匹配名字并重启其他 Worker。

验收：新 run 可证明由目标 Worker 版本生成，结果与同输入配置本地重现一致；历史 run 不覆盖。记录差异根因，而不是通过改验收文字消除差异。

## 10. R08：观察退出字段与追溯仍缺失

位置：`worker/diagnostics/optimization.py:build_optimization_plan`、`frontend/report/optimization.js:monitoring/trace`、`worker/pipeline/task_runner.py:182`。

前端读取 observation_window、exit_conditions、review_metrics、rule_version；当前生成器不输出这些字段。局部复现确认四者均缺失。_config_refs 也未覆盖所有动作所需阈值。例如加投动作主要只留 config_version，不能说明具体判断边界。

修复步骤：

1. 从原项目规则及实际生效配置定义观察窗口、退出条件和复盘指标；不得抄参考截图固定数值。
2. 同步策略定义、配置校验、策略 UI、SQL 配置校验与版本兼容，给旧配置明确迁移/默认语义。
3. 模块和动作层保留 rule_version/config_version、命中规则、实际阈值、输入来源、实体范围。
4. 观察/退出条件为结构化字段：指标、比较符、阈值、单位、周期、规则来源；前端渲染同一字段，不靠文案推算。
5. 缺配置时返回待确认与具体字段；有完整配置时应输出真实条件，不能永远 pending。

验收：每类动作至少一个样例追溯完整；改变一个阈值，能解释结论是否改变；观察/退出字段与前端读取一致。

## 11. R09：私有报告缺少重现所需快照和用量留底

pipeline 生成 rules-snapshot.json、provider-usage.json、run-meta.json 等；ProductionWorker 仅上传主表加部分模块，随后 TemporaryDirectory 被清理。bundle 主要保留版本字符串，不完整保存实际规则快照、Provider 使用明细及预算回执。provider_evidence 的 real_provider_verified 初始化后一直为 false，status=injected_data 也不能区分假注入和真实数据来源。

修复：把已校验、脱敏、必要的规则快照、模块追溯、Provider 调用摘要持久化到同一个私有 bundle 或同 task/run 的私有对象。保留输入hash、规则/配置版本与完整生效值、工具/schema版本、请求参数摘要、时间窗、调用/缓存/重试/credits、结果hash。不得保存带认证头的原始网络记录。

_USAGE_FIELDS 当前不接受 credits 类字段；应为版本化 usage/cost 扩展独立结构，不直接塞入旧严格对象。真实来源标记必须由受信任服务器适配器结合调用证据给出，不是简单改成 true。

验收：本地临时目录清理后，仍能由私有持久产物重建判断依据和核查调用量；预算耗尽/失败也有摘要，不泄露 Secret。前端否词导出的 currency_code 和版本从同一追溯来源填入，缺失不可默认 USD。

## 12. R10：统计和配置问题

### R10a 覆盖数可以为负（已复现）

`worker/report/modules.py:negative_module_status` 使用 len(records)-unknown-missing；同一行可以同时 unknown 且 required_ad_fields_missing，被扣两次。

输入 keyword=missing、clicks=null、spend=1、orders=0，实际 coverage={classified_rows:-1,total_rows:1}。

修复：按唯一行 ID 统计满足完整条件的行数；明确 classified 与 actionable 的差别。验收要求任何输入均有 0≤classified_rows≤total_rows，同一缺失行只扣一次。

### R10b 高花费百分位被硬编码

`build_negative_keywords` 的 position=(len(spends)-1)*0.75 固定75分位，无法通过策略调整。项目要求关键业务阈值配置化。

修复：加入版本化的 percentile 和样本范围配置，保留实际计算阈值、币种和算法；同步 Python/SQL/UI 校验。不擅自采用参考图的80分位。边界覆盖空样本、单样本、并列值和非法配置。

### R10c 低 CVR 高花费筛查范围不清楚

orders>0 提前进入 protected_converted 并 continue，导致虽有订单但 CVR 很低、花费很高的词不进入诊断组。保护已有订单免于否定是合理的，但不等于无需诊断。

修复：把诊断标签与否定操作资格解耦；保留零单否词保护，又能记录低CVR/高花费诊断。若 UI 必须互斥分组，设计显式优先级且不丢诊断原因；统计去重。先以项目业务口径定义样本范围，不仅为了凑更多候选。

## 13. 页面整改要求（配合 LUNU-02，不重做整站）

当前已存在共享蓝色样式和独立 HTML/JS；本轮没有重新做浏览器视觉验收，不能声称当前颜色或手机布局实测失败。

在修复数据时同步以下有源码依据的体验问题：

- 状态 reason_code 当前多处直接显示英文内部编码，如 relevance_unknown；保留展开式技术详情，主界面使用稳定中文解释与下一步处理提示。
- 报告摘要分开“已解析关键词数”“有排名数据数”“有完整判断数”；91行且排名全空应明确写0/91覆盖。
- 否词页六类桶、诊断标签、可导出数量口径一致，已转化保护与冲突说明可见。
- 广告优化页按实体显示各自事实和动作；仅关键词汇总时标清范围。
- 图片页保持自有图/竞对图一对一与证据引用对应，禁止不同竞品观察混排。
- 保留独立 HTML＋共享 CSS/鉴权/导航；不要为每个控件建HTML或改成多层iframe。
- 修改后验桌面1440px与手机390px：固定列宽、表头居中、内容左对齐、表内横向滚动，页头/导航不被宽表撑破；截图带地址栏，不能用本地 demo 冒充真实 run。

## 14. Lunu 的执行与测试办法

### 工作边界

1. 先 git status，保留用户其他修改。只在收到实施指令后按监督器协议新增/领取整改任务。
2. 每次完成一个可用修复提交一次，写明文件和相关测试；不要把“状态改成完成”作为修复。
3. 本地合成数据只放测试/临时目录，不能导入现网报告凑数。
4. 生产发布前完成本地契约、流水线、私有bundle与前端联调；外部步骤按已有授权及实际费用边界执行。
5. 无关的历史测试不反复跑；先跑受影响测试，最终集成再跑完整 UAT。

### 可复现的本地检查入口

使用已装 openpyxl 的 Python。当前机器已验证路径：

```powershell
$auditPy = 'C:\Users\Jacky\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe'
& $auditPy -m unittest worker.tests.test_production_runtime worker.tests.test_task_runner
Push-Location frontend
try { & $auditPy -m unittest test_frontend_contract } finally { Pop-Location }
& $auditPy scripts/check_frontend_syntax.py
```

前端测试必须在正确目录运行，避免把 import contract_check 路径错误误报为业务失败。上面是后续执行建议，本轮没有重跑整套 UAT。

新增或扩展真实行为测试，覆盖 R03/R04/R05/R10 的最小复现。不要只检查源码包含某字符串；至少要实际生成产物并断言值与状态。

### 最终验收清单

- [ ] R01：完成 Gate 能拒绝未接通的六模块，不再只认 passed 文本。
- [ ] R02：生产采集有真实来源；同任务的排名、市场、竞对进入最终bundle。
- [ ] R03：listing 文件进入生产bundle，视觉判断与图证据对应，ready 严格。
- [ ] R04：各实体数字独立，合计与关键词和原表一致。
- [ ] R05：相关/已转化/未知词不会被词组候选误伤，导出防线一致。
- [ ] R06：相关性确认数据可输入、可追溯、隔离正确。
- [ ] R07：Worker release 和新run绑定，可重现线上输出。
- [ ] R08：观察/退出/复盘/版本字段在后端和页面贯通。
- [ ] R09：临时文件删除后仍有持久私有证据，调用量与成本摘要可查。
- [ ] R10：覆盖数非负、阈值配置化、低CVR诊断保留正确口径。
- [ ] 新run六页逐页有数据/来源/判断或有证据的供应商空结果；实现缺口不能当供应商空结果。
- [ ] 新版本未登录/跨店拒绝与私有报告权限复验，原广告对账零差值。
- [ ] 桌面/手机截图与发布版本留底；历史run不改写。

交付时逐项写：已修复文件、实际验证命令和结果、证据位置、未完成事项。若仍有实现缺口，报告为未完成，不能再次宣布“26/26即项目完成”。
